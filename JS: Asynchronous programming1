my decision

// @ts-check
/* eslint-disable no-console */

import fs from 'fs';

// BEGIN (write your solution here)
export default (filepath) => fs.readFile(filepath, 'utf-8', (_error, data) => console.log(data));
// END
